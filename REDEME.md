# Magi 决策控制台

## 项目作用

Magi 决策控制台是一个面向自然语言问题的 AI 裁决系统。

用户提交一个问题后，系统会识别问题类型、抽取关键变量，并交由三个固定决策单元进行分析：

- `Melchior`：负责逻辑、效率、收益、成本、约束和风险判断
- `Balthasar`：负责情绪成本、关系影响和个人承受度判断
- `Casper`：负责经验模式、现实可执行性和相似历史判断

主控单元 `Magi Core` 汇总三脑结果后，输出一次明确裁决：

- `通过`
- `否决`
- `延后`
- `拒绝裁决`

系统重点不是聊天，而是将一个问题送入三脑裁决流程，完成一次结构化判断，并展示最终结论、裁决依据、分歧意见和缺失信息。

## 核心能力

- 用户注册、登录、退出
- 获取当前用户与权限能力
- 提交自然语言问题
- 自动识别问题类型
- 自动抽取关键变量
- 三脑独立分析
- 主控汇总裁决
- 保存并回看用户历史裁决
- 授权用户配置个人 AI API
- 未授权用户使用系统 AI API

## 技术结构

项目采用 monorepo 结构：

- `apps/frontend`：Taro、Vite、Less、TypeScript 前端主控台
- `apps/backend`：Fastify、Prisma、MySQL 后端服务
- `packages/shared`：前后端共享类型

前端负责页面渲染、状态展示、裁决触发、历史回看和 AI 配置入口。

后端负责认证、权限、AI API 配置加密保存、LLM 裁决编排、限流、超时降级和数据持久化。
